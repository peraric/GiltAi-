const messages=document.getElementById('messages');const input=document.getElementById('input');const history=document.getElementById('history');const welcome=document.getElementById('welcome');const composer=document.getElementById('composer');let chats=[];
function add(role,text){welcome?.remove();const row=document.createElement('div');row.className=`row ${role}`;if(role==='ai')row.innerHTML='<div class="ai-icon">G</div><div class="bubble"></div>';else row.innerHTML='<div class="bubble"></div>';row.querySelector('.bubble').textContent=text;messages.appendChild(row);messages.scrollTop=messages.scrollHeight}
function answer(q){const text="I’m GiltAi. I’m ready to help you think, write, code, explain, plan, and create.\n\nThis is the independent GiltAi prototype, so no external AI provider is connected yet. The product shell and conversation experience are ready for the GiltAi engine.";setTimeout(()=>add('ai',text),350)}
function send(q){q=q.trim();if(!q)return;add('user',q);chats.unshift(q);history.innerHTML=chats.slice(0,6).map(x=>`<button>${escapeHtml(x.slice(0,38))}</button>`).join('');input.value='';input.style.height='auto';answer(q)}
function escapeHtml(s){return s.replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]))}
composer.addEventListener('submit',e=>{e.preventDefault();send(input.value)});input.addEventListener('input',()=>{input.style.height='auto';input.style.height=Math.min(input.scrollHeight,140)+'px'});input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send(input.value)}});document.querySelectorAll('[data-prompt]').forEach(b=>b.addEventListener('click',()=>send(b.dataset.prompt)));document.getElementById('newChat').addEventListener('click',()=>location.reload());
